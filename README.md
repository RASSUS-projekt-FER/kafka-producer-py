Make this working dir
### Create python virtual env
`mkdir ../venv`
`virtualenv ../venv`

### Start virtualenv
`../venv/Scripts/activate`

### Install requirements in venev
`pip install -r requirements.txt`

### Start service
`python device.py`